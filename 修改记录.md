# 修改记录 / CHANGELOG — portfolio

## 2026-08-29 · 修复损坏文件 + 恢复自动化同步 + 子项目同步核查

> 备份：执行前已全量备份至 `C:\Users\ASUS\Desktop\portfolio-backup-20260829`（171 文件 / 21.7MB / robocopy 0 失败）。

### 一、删除损坏文件（6 个，内容均为误存的 HTML）
- `projects/portfolio-source/service-worker.js`
- `projects/portfolio-source/sw.js`
- `projects/portfolio-source/manifest.json`
- `projects/portfolio-source/manifest.webmanifest`
- `projects/delta-force/service-worker.js`
- `projects/delta-force/manifest.webmanifest`

成因：`restore-projects.ps1` 下载时不校验 HTTP 状态码，把部署站的 404 回退页存成了假文件。

### 二、脚本与配置修复
1. **`restore-projects.ps1`**：`Get-File` 增加 `curl -w '%{http_code}'` 状态码校验，非 200 不落盘并打印 `[SKIP]`。
2. **`projects.json`**：
   - ping-pong-duel 的 source 由 `../乒乓对决_合并/public`（已不存在，导致 `npm run deploy` 失败）改为 `projects/ping-pong-duel`（projects/ 内维护）；
   - 新增 `space-kill` 条目，收集配置与首页 4 个作品卡片一致。
3. **`.gitignore`**：新增排除 `.collect-temp/`。

### 三、delta-force 副本同步（`delta-force-deploy` 独立仓库 → portfolio）
portfolio 内副本停留在 `v20260730m`，独立仓库已演进到 `v20260803n / v20260805w`（含 SW v2 缓存修复、定时器降频、滚动条适配）。已同步并逐文件 MD5 校验一致：

| 文件 | 说明 |
|---|---|
| `index.html` | 新版（另做内嵌适配，见下） |
| `sw.js` | STATIC_CACHE v1 → v2，强制旧缓存失效 |
| `js/bundle.js` | 新构建产物 |
| `js/sw-register.js` | 新版 + 补回内嵌适配 |
| `css/layout.css` `css/components.css` | v20260803n |
| `manifest.json` | 新版 |
| `data/metadata.json` | **新增**（425KB）—— 修复 `/api/metadata` 静态回退失效 |

**内嵌适配（portfolio 专属差异，同步时必须保留，勿被独立仓库覆盖）**：
- `js/sw-register.js`：加回 `window.self !== window.top` 守卫；SW 注册路径由根绝对 `/sw.js?v=…` 改回相对 `sw.js?v=v20260805w`（根绝对路径会在作品集 iframe 内劫持全站）；
- `index.html`：favicon / manifest / preload 的 3 处 `/xxx` 根绝对路径改为相对路径。
- `functions/api/[[path]].js` 两仓库仅一行合法差异（metadata.json 静态回退路径各自适配所在域名），portfolio 侧正确，未改动。
- **不要**把 deploy 仓库的 `functions/index.js` 复制进 portfolio——它是独立域名的根路径 302 缓存破除，放进 portfolio 会劫持作品集首页。

### 四、四个子项目自动化同步核查结论

| 项目 | 同步方式 | 状态 |
|---|---|---|
| ping-pong-duel | 无本地源仓库（`乒乓对决_合并` 已删除），portfolio 内为**唯一本地副本** | ⚠️ 自维护；建议推送 Gitee/GitHub 后改 git 源 |
| delta-force | 手动从 `delta-force-deploy` 同步 | ✅ 本次已同步至 v20260805w 并补 data/ |
| portfolio-source | projects/ 内维护 | ✅ 正常；且确认 `js/main.js`/`css/style.css`/`projects.json` 被其展示页 fetch 引用，**不是死代码**，不可删除 |
| space-kill | 手动（本次已纳入 projects.json） | ✅ 与 `Desktop\space-kill\公测2.0` 内容一致；仅 `规则.docx` 差 2 字节（portfolio 版 8/27 更新，较源仓库 8/25 新，以 portfolio 为准）；`人类/异形/外星人胜利线.docx` 为 portfolio 独有剧情交付物 |

### 五、验证结果
- `node build-collect.js`：4/4 成功，EXIT=0（修复前 1/2 失败）。
- 同步后 8 个文件 MD5 与 `delta-force-deploy` 完全一致。
- `git status` 变更清单与本次记录逐项对应（6 D / 9 M / 1 ??）。

---

## 2026-08-29 · 建立项目自动化同步流程（space-kill 本次屏蔽）

### 新增文件
- **`sync.json`** —— 同步配置（源目录 / 目标目录 / 文件清单 / 补丁规则 / 禁复制清单 / 各项目开关）。
- **`sync-projects.js`** —— 同步执行器，读取 `sync.json`：复制 → 打内嵌适配补丁 → 输出统计。
- `package.json` 新增脚本：`npm run sync`（执行）、`npm run sync:dry`（预览不写盘）。

### 使用方式
```powershell
npm run sync          # 执行同步
npm run sync:dry      # 只预览，不写磁盘
npm run sync && npm run deploy   # 同步后部署（sync 故意未并入 deploy，避免换机器时源目录缺失导致部署被阻断）
```

### 当前开关状态
| 项目 | 状态 | 说明 |
|---|---|---|
| delta-force | ✅ enabled | `../delta-force-deploy` → `projects/delta-force`，10 个文件 + 6 条补丁 |
| ping-pong-duel | ⏭ disabled | 无本地源仓库，需先推送远程再配置 `sourceDir` |
| portfolio-source | ⏭ disabled | 无外部来源，projects/ 内维护 |
| space-kill | ⏭ **disabled** | **本次按要求暂时屏蔽**；恢复方式：置 `enabled: true` 并补 `sourceDir`（`../space-kill`）与 `files` 列表 |

### 内置的安全机制
1. **禁复制清单校验**：配置 `neverCopy` 中的文件若被误列入 `files`，脚本报错退出 —— 防止 `functions/index.js`（根路径 302，会劫持作品集首页）和 `functions/api/[[path]].js`（两仓库有一行合法差异）被覆盖。
2. **内嵌适配补丁**：同步后自动把 deploy 独立站版本改成作品集可用版本，规则写在配置里，可随上游改版随时增删：
   - `index.html`：4 处根绝对路径改相对（`delta-force-logo.webp` / `manifest.json` / `icon.ico` / `icon-192.png`）；
   - `js/sw-register.js`：补回 `window.self !== window.top` iframe 守卫；`register('/sw.js?v=…')` → `register('sw.js?v=…', { scope: './' })`（根绝对路径会在作品集 iframe 内劫持全站）。
3. **补丁未匹配即告警**：上游改版导致规则失配时打印 `⚠ 补丁未匹配（请人工检查）` 并计入统计（不静默通过）。
4. **幂等**：每次「从源复制 → 重打补丁」，结果稳定，不会累积重复代码（已验证守卫计数恒为 1）。
5. **`--dry-run`**：只预览不写盘。

### 本次同步实测
- 首次运行即捕获到我手动同步后源仓库的**又一次新构建**（index.html 32561→36779，sw.js→6173，bundle.js→154872），验证自动化价值。
- 新构建引入了新的根绝对路径 `/icon.ico`、`/icon-192.png`，已补入补丁规则并把两个图标纳入同步清单。
- 连续两次执行：`更新 4 → 更新 2 / 无变化 8`，`补丁 6`，守卫计数恒为 1，无重复注入。
- 收尾验证：index.html 已无根绝对路径；`build-collect.js` 仍 4/4 通过。

### 后续可选项
- ping-pong-duel 推向远程后，把 `sync.json` 对应条目 `enabled: true` 并填 `sourceDir` / `files`，即可纳入统一同步。
- 若希望部署时自动同步，可把 `deploy` 脚本改为 `npm run sync && npm run collect && ...`（当前未启用，避免耦合）。
